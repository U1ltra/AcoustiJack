Add the `libActorControllerPlugin.so` file found in `install/ActorControllerPlugin/lib` to `~/.gz/sim/plugins/`.

Test plugin by running the `default_ros.sdf` world found in this repository. Run `ros2 run teleop_twist_keyboard teleop_twist_keyboard` to move the actor around in the environment.
